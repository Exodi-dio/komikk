# Gradle daemon JVM: use any JDK 21. Vendor is intentionally not pinned so
# auto-detected or JAVA_HOME JDKs satisfy the spec instead of provisioning
# a specific distribution via foojay.
toolchainVersion=21